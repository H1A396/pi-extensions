# pi-extensions 开发与部署规划（临时）

> 本文件为临时规划文档，确认后转正为正式规则（可并入 AGENTS.md 或独立 GIT.md）。

## 一、路径模型

### 开发根目录：`pi-extensions/`（git 仓库根）

```
pi-extensions/                        ← 扩展开发根目录（在此进行 git 管理）
├── AGENTS.md                         ← 项目规则
├── 开发部署规划.md                    ← 本文档
└── pi-myqy-<扩展名>/                 ← 每个单独扩展一个文件夹
    ├── pi-myqy-<扩展名>.ts           ← 扩展源码（命名规则见 AGENTS.md）
    ├── example-config.json           ← 配置示例（可选）
    └── README.md
```

### pi 运行时扩展根目录：`~/.pi/agent/extensions/`

```
~/.pi/agent/extensions/               ← pi 扩展根目录（同样为"根 + 单独扩展文件夹"结构）
└── pi-myqy-<扩展名>/                 ← 与开发目录一一对应
    └── index.ts                      ← 从源码复制的入口文件
```

**为什么运行时入口叫 `index.ts`**：pi 自动发现规则（已验证源码 `extensions/loader.js`）支持三种形态：

| 形态 | 路径 |
|---|---|
| 直接文件 | `extensions/*.ts` |
| 子目录 | `extensions/<dir>/index.ts`（或 `index.js`） |
| 包声明 | `extensions/<dir>/package.json` 的 `pi` 字段 |

因此采用"扩展文件夹 + `index.ts`"结构可被自动发现。

## 二、Git 管理

- 仓库根：`pi-extensions/`（`git init` 在此执行）
- **入库**：源码、README、示例配置、部署脚本、AGENTS.md、规划文档
- **不入库**：`~/.pi/agent/` 下的一切（运行时目录、个人配置 `pi-myqy-model-filters.json`）
- `.gitignore` 预留：`node_modules/`、`dist/`、`.DS_Store`
- 提交约定：按逻辑分组，简短中文描述，可用 `feat:` / `fix:` / `docs:` / `chore:` 前缀

## 三、部署（复制模式）

**开发根目录与 pi 运行时目录通过"复制"关联，而非软链接**：

```
开发（git 仓库内）                              pi 运行时
pi-extensions/pi-myqy-<name>/             →     ~/.pi/agent/extensions/pi-myqy-<name>/
  └── pi-myqy-<name>.ts  ──复制──►            └── index.ts
```

复制模式特性：

- 源码只有一份（仓库内），运行时是部署副本
- **修改源码后不会自动同步**，必须重新执行部署，再 `/reload` 生效
- 副本内容以部署脚本为准，避免手工同步出错

### 部署脚本 `deploy.sh`（位于 pi-extensions 根）

```bash
#!/usr/bin/env bash
# 将 pi-extensions 下所有 pi-myqy-* 扩展复制到 pi 运行时扩展目录
set -euo pipefail

SRC="$(cd "$(dirname "$0")" && pwd)"
DEST="${HOME}/.pi/agent/extensions"

for dir in "$SRC"/pi-myqy-*; do
  [ -d "$dir" ] || continue
  name="$(basename "$dir")"
  mkdir -p "$DEST/$name"
  cp "$dir"/*.ts "$DEST/$name/index.ts"
  echo "deployed: $name → $DEST/$name/index.ts"
done
```

## 四、标准开发流程

1. 在 `pi-extensions/pi-myqy-<name>/` 修改源码（开发路径固定在这里）
2. 运行 `./deploy.sh` 部署到 `~/.pi/agent/extensions/`
3. pi 中 `/reload` 使新副本生效
4. `git add` + `git commit` 记录变更（提交到 pi-extensions 仓库）

## 五、新机器部署

1. clone 仓库（pi-extensions）
2. 运行 `./deploy.sh` 复制扩展到运行时目录
3. 从 `example-config.json` 复制生成个人配置 `~/.pi/agent/pi-myqy-model-filters.json`
4. 重启 pi

## 六、当前状态与迁移

现有扩展 `pi-myqy-model-filter` 按本模式迁移：

| 项目 | 现状 | 目标 |
|---|---|---|
| 开发源码 | `pi-extensions/pi-myqy-model-filter/pi-myqy-model-filter.ts` | 不变（已符合） |
| 运行时 | `~/.pi/agent/extensions/pi-myqy-model-filter.ts`（平铺软链接） | `~/.pi/agent/extensions/pi-myqy-model-filter/index.ts`（文件夹副本） |
| 配置 | `~/.pi/agent/pi-myqy-model-filters.json` | 不变（仓库外） |

待办清单：

- [ ] `git init`（pi-extensions 根）
- [ ] 创建 `.gitignore`
- [ ] 创建 `deploy.sh` 并 `chmod +x`
- [ ] 迁移运行时为文件夹副本结构（替换现有软链接）
- [ ] 创建 `example-config.json`
- [ ] 首次 git 提交
